namespace FallingBlocks.Logic
{
    /// <summary>7 種類のテトリミノ。盤面に置くときは (int)type + 1 を値として使う。</summary>
    public enum TetrominoType
    {
        I,
        O,
        T,
        S,
        Z,
        J,
        L,
    }
}
